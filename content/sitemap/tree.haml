- items = @items.reject{|i|i.binary?}
- root = items.find {|i|i.identifier == '/'}
- _erbout << traverse_items([root], :ul, :li) do |i|
  - "#{link(i)} <span>(last modified: #{i[:mtime].strftime('%Y-%m-%d')})</span>"
