#!/usr/bin/env ruby
require 'haml'

compile '*' do
  filter :haml
  layout 'default'
  filter :relativize_paths, :type => :html
end

route '*' do
  item.identifier + 'index.html'
end

layout '*', :haml
